# Vantage Labs — SEO Checklist

> Generated by 4-agent SEO audit · June 2026
> Updated: Session 8 — 2026-06-09
> Check off each item as it is completed.

---

## 🔴 CRITICAL — Fix First

### Metadata & Open Graph

- [x] Add `metadata` export to `app/page.tsx` (unique title + description) — covered by root `app/layout.tsx` metadata
- [x] Add `metadata` export to `app/services/page.tsx` (unique title + description) — done via `app/services/layout.tsx`
- [x] Add `metadata` export to `app/work/page.tsx` (unique title + description) — done via `app/work/layout.tsx`
- [x] Add `metadata` export to `app/contact/page.tsx` (unique title + description) — done via `app/contact/layout.tsx`
- [x] Add `generateMetadata()` to `app/projects/[slug]/page.tsx` using project name/description — done via `app/projects/[slug]/layout.tsx`
- [ ] Add `metadataBase` to root `app/layout.tsx` metadata (required for absolute OG URLs)
- [x] Add `openGraph` block to root `app/layout.tsx` — title, description, type, locale ← DONE (Session 8)
- [ ] Add `twitter` block to root `app/layout.tsx` — card type, title, description, image
- [ ] Add `openGraph` override to each page-level metadata export
- [ ] Add `alternates.canonical` to root metadata and each page

### Robots & Sitemap

- [ ] Create `app/robots.ts` — auto-generates robots.txt with allow/disallow rules
- [ ] Create `app/sitemap.ts` — dynamic sitemap including all `/projects/[slug]` URLs

### Rendering Strategy

- [ ] Remove `'use client'` from `app/page.tsx` — move client logic to child components
- [ ] Remove `'use client'` from `app/work/page.tsx` — move client logic to child components
- [ ] Remove `'use client'` from `app/contact/page.tsx` — move client logic to child components
- [ ] Remove `'use client'` from `app/projects/[slug]/page.tsx` — make it a Server Component
- [ ] Add `generateStaticParams()` to `app/projects/[slug]/page.tsx` to statically generate all project pages

### Footer (Critical Gaps)

- [x] Add internal nav links to `components/Footer.tsx` — Work / Services / Contact ← DONE (Session 8)
- [ ] Add full NAP to footer: street address (currently only "BC, Canada")
- [ ] Add phone number to footer
- [x] Add social media links to footer — LinkedIn added ← DONE (Session 8)

---

## 🟠 HIGH PRIORITY

### Structured Data (JSON-LD)

- [ ] Add `Organization` JSON-LD schema to `app/layout.tsx` — name, logo, url, sameAs (social URLs)
- [ ] Add `LocalBusiness` JSON-LD schema — address, phone, service area (BC, Canada)
- [ ] Add `BreadcrumbList` JSON-LD schema to `app/projects/[slug]/page.tsx`
- [ ] Add `Service` JSON-LD schema to `app/services/page.tsx`

### Heading Structure

- [ ] Add `<h2>` to `components/Services.tsx` — "What We Build" label is a `<span>`, not a heading
- [ ] Add `<h2>` to `components/Work.tsx` — "Selected Work" label is a `<span>`, not a heading
- [ ] Wrap Overview, Tools & Stack, and Features labels in `<h2>` tags in `app/projects/[slug]/page.tsx`

### About Page

- [ ] Create `app/about/page.tsx` — full company story, team info, credentials, location detail
- [ ] Add `metadata` export to the new `/about` page
- [ ] Add link to `/about` in `components/Footer.tsx`
- [ ] Consider adding "About" to `components/Navbar.tsx`

### Social Media

- [x] Add LinkedIn profile link to `components/Footer.tsx` ← DONE (Session 8)
- [ ] Add Instagram (or active platforms) link to `components/Footer.tsx`
- [ ] Ensure all external social links use `rel="noopener noreferrer"`
- [ ] Add social profile URLs to `Organization` JSON-LD `sameAs` array

### Performance — Render Blocking

- [ ] Fix `app/layout.tsx` — move Calendly CSS `<link>` out of `<head>` or load it only on `/contact`
- [ ] Move Calendly `<Script>` from global layout to `app/contact/page.tsx` (currently loads on every page)

---

## 🟡 MEDIUM — SEO Polish

### Site Architecture & Internal Linking

- [ ] Add "View All Projects →" CTA at bottom of `components/Work.tsx` linking to `/work`
- [ ] Fix `components/Navbar.tsx` — change "Work" link from conditional `/#work` anchor to `/work` route
- [ ] Add internal link from `app/services/page.tsx` intro section to `/work` (show proof of work)
- [ ] Add internal link from `app/work/page.tsx` to `/services` (redirect interest to services)
- [ ] Add breadcrumb nav component to `app/projects/[slug]/page.tsx` (Home → Work → Project Name)

### Custom 404 Page

- [ ] Create `app/not-found.tsx` with branding, helpful message, and links back to key pages

### next.config.ts Optimizations

- [ ] Add security headers — `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`
- [ ] Add cache-control headers for static assets
- [ ] Add www → non-www redirect rule (or vice versa, pick one canonical domain)
- [ ] Configure `images.domains` if external images are added in the future

### NAP Consistency

- [ ] Verify business name, address, and phone are identical across footer, about page, and JSON-LD schema
- [ ] Add email to JSON-LD `LocalBusiness` schema (already in footer as mailto link)

### Images & Alt Text

- [ ] Add project screenshot/mockup images to project detail pages using `next/image` with descriptive `alt` text
- [ ] Add team or office photo to `/about` page using `next/image` with `alt` text
- [ ] Use `priority` prop on any above-the-fold `next/image` usage

---

## 🔵 LOWER PRIORITY — Long-Term Growth

### Blog / Content Marketing

- [ ] Create `app/blog/page.tsx` — blog listing page
- [ ] Create `app/blog/[slug]/page.tsx` — individual blog post template
- [ ] Add `Article` JSON-LD schema to blog post pages
- [ ] Write first 3 posts targeting keywords like "custom web design BC", "Next.js agency Canada", "web development for small business"
- [ ] Add blog link to navbar and footer once published
- [ ] Include `app/blog` in the sitemap route

### FAQ Section

- [x] Add FAQ section to homepage with `FAQPage` JSON-LD schema — FAQ accordion added to homepage (Session 8). JSON-LD schema still TODO.
- [ ] Add FAQ section to `app/services/page.tsx` with `FAQPage` JSON-LD schema
- [ ] Target common questions: pricing, timeline, tech stack, revision policy

### Misc

- [ ] Add `public/manifest.json` for PWA/installability support
- [ ] Verify `<html lang="en">` is correct locale (already set in `app/layout.tsx` — just confirm)
- [ ] Add Google Search Console verification meta tag to root `app/layout.tsx` once domain is live
- [ ] Submit sitemap URL to Google Search Console after launch
- [ ] Set up Google Business Profile listing for local SEO (BC, Canada)

---

## ✅ Already Done — No Action Needed

- [x] `font-display: 'swap'` configured on Inter font — good for CLS (`app/layout.tsx:12`)
- [x] `lang="en"` set on `<html>` tag
- [x] `aria-hidden="true"` on all decorative SVGs
- [x] Descriptive project URL slugs (`/projects/clearview-dental`, `/projects/saffron-kitchen`, etc.)
- [x] CTA buttons consistently link to `/contact` across all pages
- [x] "Back to Work" link on every project detail page
- [x] Calendly `<Script>` uses `strategy="lazyOnload"` (non-blocking JS)
- [x] Body copy is keyword-rich and natural (no stuffing detected)
- [x] All external links on project detail pages use `rel="noopener noreferrer"`
- [x] `<h1>` present on homepage (Hero.tsx), `/services`, `/work`, and project detail pages
- [x] Root metadata title + description + openGraph added (`app/layout.tsx`) — Session 8
- [x] Page-specific metadata added for `/services`, `/work`, `/contact`, `/projects/[slug]` — Session 8
- [x] Footer internal nav links added (Work / Services / Contact) — Session 8
- [x] LinkedIn social link in footer — Session 8
- [x] FAQ section on homepage (5 SMB Q&As) — Session 8

---

## Quick Stats

| Category | Total Tasks | Done |
|----------|-------------|------|
| Critical | 17 | 8 |
| High Priority | 15 | 2 |
| Medium | 14 | 0 |
| Low / Long-term | 12 | 1 |
| **Total** | **58** | **11** |

> Update the table above as tasks are completed.
